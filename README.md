# Rust-Scaffold

## Usage

### debug running

```shell
make debug
```

### build target

```shell
make dev
```

### build target with `--release` mode

```shell
make release
```

### build docker image

```shell
make image
```

### clean all target files

```shell
make clean
```

